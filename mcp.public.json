{
  "mcpServers": {
    "sql-agent": {
      "command": "uv",
      "args": ["run", "python", "-m", "backend.app.mcp.public_server"],
      "env": {
        "UV_CACHE_DIR": ".uv-cache"
      }
    }
  }
}
