# Business Rules

Place business rule documents in this directory.

Supported file types:

- `.md`
- `.txt`
- `.yaml`
- `.yml`
- `.json`

The rule search tool is intentionally limited to this directory. It rejects absolute paths, parent-directory escapes, symlinks that resolve outside the directory, oversized files, and unsupported extensions.
